"""Core proto package — canonical location for protobuf source and stubs.
core proto 包 — protobuf 源文件与生成桩代码的规范位置。

The ``.proto`` source files and the generated Python stubs now both live in
``core/proto/``.
Run ``bash core/proto/generate.sh`` to regenerate the stubs from source.
``.proto`` 源文件与生成后的 Python 桩代码现在都位于 ``core/proto/``。
运行 ``bash core/proto/generate.sh`` 从源码重新生成桩代码。
"""
