# VL 复盘（连接测试 + 单消息复盘）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 VL 大模型二次确认增加「设置页测试连接按钮」与「消息页单消息 VL 复盘按钮」，让用户能验证接入的 VL 后端是否正确、并对历史告警重跑一次 VL 判定（只展示结果，不改消息状态）。

**Architecture:** 复用 `core/vl_confirm.py` 的 `VLConfirmClient`（OpenAI 兼容），新增 `complete()`（返回原始文本、异常上抛）与合成测试图生成器；两个后端端点（`POST /api/settings/vl/test`、`POST /api/messages/{id}/vl-review`）显式把上游错误转为 502；消息链路（核心处理器消息 dict → `AnalysisMessage` schema → WS/持久化 → 列表 API JOIN `video_sources`）补充 `scene_id`，供前端按插件启用状态控制按钮显隐。

**Tech Stack:** Python 3.11 / FastAPI / Pydantic v2 / pytest（`uv run pytest`）/ Vue 3 + Pinia + Element Plus / Vitest + jsdom。

## Global Constraints

- 设计文档：`docs/superpowers/specs/2026-09-01-vl-review-design.md`（行为以它为准）。
- `VLConfirmClient.confirm()` 对外行为**必须不变**：任何异常 → `None`（fail-open），供生产处理器继续静默降级。
- 两个新端点必须显式暴露上游错误：上游异常 → `HTTPException(502)`，`detail` 形如 `VL request failed: <原因>`。
- 权限：`/api/settings/vl/test` 用 `require_any_permission("settings:*", "settings:plugins")`；`/api/messages/{message_id}/vl-review` 用 `require_permission("messages:annotate")`（复用现有通配 `messages:*`，不改角色目录）。
- 复盘端点**不得修改** `analysis_messages` 任何字段（尤其 `false_positive`）。
- 复盘取图规则：按 `<scene>_vl_confirm_image_source` 配置，`"annotated"` → `kind="detected"`，否则 `kind="original"`；**不做 ROI 裁剪**（发全图）。
- prompt 回退与处理器一致：`<scene>_vl_confirm_prompt` 为空 → 对应场景 `DEFAULT_VL_CONFIRM_PROMPT`；response_key 为空 → 对应场景 `DEFAULT_VL_CONFIRM_RESPONSE_KEY`（`core/smoke/constants.py` / `core/fire_door/constants.py`）。
- 每个新路由必须在 `backend/audit.py` 的 `AUDIT_OPERATION_MAP` 登记：`("POST", "/api/settings/vl/test"): ("settings.vl_test", "settings")`、`("POST", "/api/messages/{message_id}/vl-review"): ("messages.vl_review", "messages")`。
- i18n：所有新文案必须同时加入 `frontend/src/i18n/locales/zh-CN.js` 与 `en-US.js`，键名一致。
- 后端测试命令：`uv run pytest tests/<file>::<test> -q`；全套 `uv run pytest tests -q`（基线：404 passed + 1 个既有失败 `tests/test_main.py::TestFrontendFallbackRoutes::test_direct_frontend_route_serves_index_html`，与本计划无关）。
- 前端测试命令：`cd frontend && npx vitest run`（基线：71 passed + 2 个既有失败 `settingsRoutes.test.js`，与本计划无关）；`cd frontend && npm run build` 必须通过。
- 提交信息风格：`feat/fix/test(scope): 简述`（与仓库现有提交一致）。

## 文件结构

| 文件 | 责任 | 动作 |
|---|---|---|
| `core/vl_confirm.py` | VL 客户端：`complete()` 原始调用、`build_vl_test_image_data_url()`、`encode_frame_as_data_url()` 公开化 | 修改 |
| `core/smoke/processor.py` | 消息 dict 加 `scene_id: "smoke"` | 修改 |
| `core/fire_door/processor.py` | 消息 dict 加 `scene_id: "fire_door"` | 修改 |
| `backend/models/schemas.py` | `AnalysisMessage.scene_id` 字段；`VlTestRequest` 模型 | 修改 |
| `backend/processing/base.py` | dict→`AnalysisMessage` 转换透传 `scene_id` | 修改 |
| `backend/db/database.py` | `list_analysis_messages` LEFT JOIN 出 `scene_id`；新增 `get_analysis_message_review_context()` | 修改 |
| `backend/api/settings.py` | `POST /vl/test` 端点 | 修改 |
| `backend/api/messages.py` | `POST /{message_id}/vl-review` 端点 | 修改 |
| `backend/audit.py` | 登记 2 条审计映射 | 修改 |
| `tests/test_vl_confirm.py` | `complete()` / 测试图 / `confirm()` 行为不变 | 修改 |
| `tests/test_smoke.py` / `tests/test_fire_door.py` | 消息 dict 含 `scene_id` | 修改 |
| `tests/test_settings.py` | `/api/settings/vl/test` 端点 | 修改 |
| `tests/test_messages.py` | `/api/messages/{id}/vl-review` 端点 + 列表 `scene_id` | 修改 |
| `frontend/src/api/index.js` | `settingsApi.testVl`、`messagesApi.vlReview` | 修改 |
| `frontend/src/stores/appSettings.js` | `testVl` action | 修改 |
| `frontend/src/stores/message.js` | `vlReviewMessage` action | 修改 |
| `frontend/src/stores/__tests__/message.test.js` | `vlReviewMessage` 测试 | 修改 |
| `frontend/src/views/Settings.vue` | 两个 VL 卡片「测试连接」按钮 | 修改 |
| `frontend/src/components/MessageList.vue` | 「VL 复盘」按钮 + 显隐逻辑 | 修改 |
| `frontend/src/views/Messages.vue` | 复盘 handler + 结果对话框 + per-message loading | 修改 |
| `frontend/src/i18n/locales/zh-CN.js` / `en-US.js` | 新文案 | 修改 |

---

### Task 1: `core/vl_confirm.py` — `complete()` + 合成测试图

**Files:**
- Modify: `core/vl_confirm.py`
- Test: `tests/test_vl_confirm.py`

**Interfaces:**
- Consumes: 现有 `VLConfirmClient.__init__(base_url, api_key, model, timeout=60)`、`parse_vl_response(text, response_key)`。
- Produces（后续任务依赖的精确签名）:
  - `VLConfirmClient.complete(self, image_data_url: str, prompt: str) -> str` — 返回模型原始文本；任何失败抛异常。
  - `build_vl_test_image_data_url() -> str` — 返回 `data:image/jpeg;base64,...`。
  - `encode_frame_as_data_url(frame: np.ndarray) -> str` — 原私有 `_encode_frame_as_data_url` 公开化（内部调用点同步改名）。
  - 常量 `VL_TEST_PROMPT: str`。
  - `confirm()` 签名与行为不变。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_vl_confirm.py` 末尾，并更新文件顶部 import 行加入 `build_vl_test_image_data_url, encode_frame_as_data_url, VL_TEST_PROMPT`）

```python
async def test_complete_returns_raw_text():
    client = VLConfirmClient("http://localhost:30000/v1", "EMPTY", "/models/Mage-VL")
    mock_response = MagicMock()
    mock_response.choices = [MagicMock()]
    mock_response.choices[0].message.content = "hello raw"
    client._client = AsyncMock()
    client._client.chat.completions.create = AsyncMock(return_value=mock_response)

    raw = await client.complete("data:image/jpeg;base64,abc", "Ping")
    assert raw == "hello raw"


async def test_complete_raises_on_upstream_error():
    client = VLConfirmClient("http://localhost:30000/v1", "EMPTY", "/models/Mage-VL")
    client._client = AsyncMock()
    client._client.chat.completions.create = AsyncMock(side_effect=Exception("conn refused"))

    with pytest.raises(Exception, match="conn refused"):
        await client.complete("data:image/jpeg;base64,abc", "Ping")


async def test_complete_empty_content_returns_empty_string():
    client = VLConfirmClient("http://localhost:30000/v1", "EMPTY", "/models/Mage-VL")
    mock_response = MagicMock()
    mock_response.choices = [MagicMock()]
    mock_response.choices[0].message.content = None
    client._client = AsyncMock()
    client._client.chat.completions.create = AsyncMock(return_value=mock_response)

    assert await client.complete("data:image/jpeg;base64,abc", "Ping") == ""


async def test_confirm_behavior_unchanged_uses_complete():
    client = VLConfirmClient("http://localhost:30000/v1", "EMPTY", "/models/Mage-VL")
    client.complete = AsyncMock(return_value='{"smoke": true}')
    assert await client.confirm("x", "P", "smoke") is True
    client.complete = AsyncMock(side_effect=Exception("boom"))
    assert await client.confirm("x", "P", "smoke") is None


def test_build_vl_test_image_data_url_is_valid_jpeg():
    url = build_vl_test_image_data_url()
    assert url.startswith("data:image/jpeg;base64,")
    decoded = _decode_data_url(url)
    assert decoded.shape == (240, 320, 3)


def test_encode_frame_as_data_url_roundtrip():
    frame = np.zeros((30, 40, 3), dtype=np.uint8)
    decoded = _decode_data_url(encode_frame_as_data_url(frame))
    assert decoded.shape == (30, 40, 3)


def test_vl_test_prompt_mentions_json():
    assert "connected" in VL_TEST_PROMPT
```

注意：`tests/test_vl_confirm.py` 顶部需要新增 `import pytest`（当前未导入）。import 行改为：

```python
from core.vl_confirm import (
    VLConfirmClient,
    VL_TEST_PROMPT,
    build_vl_image_data_url,
    build_vl_test_image_data_url,
    crop_roi_image,
    encode_frame_as_data_url,
    parse_vl_response,
)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_vl_confirm.py -q`
Expected: 新用例 FAIL（`ImportError: cannot import name 'build_vl_test_image_data_url'`），旧用例 PASS。

- [ ] **Step 3: 实现**（修改 `core/vl_confirm.py`）

1) 在 `parse_vl_response` 之前新增常量：

```python
VL_TEST_PROMPT = (
    "This is a connection test image. 这是一张连通性测试图。"
    "Reply with ONLY: {\"connected\": true}"
)
```

2) 把 `_encode_frame_as_data_url` 改名为 `encode_frame_as_data_url`（公开），并更新 `crop_roi_image` 内部的两处调用。

3) 新增合成测试图生成器（放在 `encode_frame_as_data_url` 之后）：

```python
def build_vl_test_image_data_url() -> str:
    """Build a deterministic synthetic test image as a JPEG data URL.
    生成确定性合成测试图并编码为 JPEG data URL。"""
    frame = np.full((240, 320, 3), 200, dtype=np.uint8)
    frame[80:160, 110:210] = (255, 0, 0)
    return encode_frame_as_data_url(frame)
```

4) `VLConfirmClient` 新增 `complete()`，并把 `confirm()` 重构为基于它：

```python
    async def complete(self, image_data_url: str, prompt: str) -> str:
        """Send an image + prompt to the model and return the raw text.

        Unlike :meth:`confirm`, any failure is raised to the caller so
        endpoints can surface the concrete upstream error.
        与 ``confirm`` 不同，任何失败都会抛给调用方，便于端点展示具体错误。
        """
        content = [
            {"type": "image_url", "image_url": {"url": image_data_url}},
            {"type": "text", "text": prompt},
        ]
        response = await self._client.chat.completions.create(
            model=self._model,
            messages=[{"role": "user", "content": content}],
            max_tokens=50,
            temperature=0,
        )
        return response.choices[0].message.content or ""

    async def confirm(
        self,
        image_data_url: str,
        prompt: str,
        response_key: str,
    ) -> bool | None:
        """Send an image to the VL model and parse the confirmation result.

        Returns ``True`` (confirmed), ``False`` (rejected), or ``None`` on
        error / unparseable output (callers should fail-open).
        """
        try:
            raw = await self.complete(image_data_url, prompt)
            logger.debug("VL confirm raw response: {}", raw)
            return parse_vl_response(raw, response_key)
        except Exception:
            logger.warning("VL confirm failed", exc_info=True)
            return None
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_vl_confirm.py -q`
Expected: 全部 PASS（旧用例保持通过，`confirm()` 行为未变）。

- [ ] **Step 5: 全套回归 + 提交**

Run: `uv run pytest tests -q`（确认无新增失败，既有 1 个 `test_main.py` 失败除外）

```bash
git add core/vl_confirm.py tests/test_vl_confirm.py
git commit -m "feat(vl): add complete() raw call and synthetic test image builder"
```

---

### Task 2: `scene_id` 生产侧 — schema + 两个核心处理器

**Files:**
- Modify: `backend/models/schemas.py`（`AnalysisMessage`，约 461 行）
- Modify: `core/smoke/processor.py:141-151`（消息 dict）
- Modify: `core/fire_door/processor.py:205-221`（消息 dict）
- Modify: `backend/processing/base.py:114-133`（dict→`AnalysisMessage` 转换）
- Test: `tests/test_smoke.py`、`tests/test_fire_door.py`

**Interfaces:**
- Produces: `AnalysisMessage.scene_id: str = "smoke"`（WS 广播与持久化 dump 自动携带）；两个处理器消息 dict 含 `"scene_id"`。

- [ ] **Step 1: 写失败测试**

`tests/test_smoke.py` 约 56-57 行已有对 `result.messages[0]` 的断言（`assert result.messages[0]["level"] == "alert"`），在其后追加：

```python
        assert result.messages[0]["scene_id"] == "smoke"
```

`tests/test_fire_door.py` 的 `test_vl_confirm_allows_alarm_when_model_returns_true` 用例（约 277-295 行）末尾已有：

```python
    assert result.messages[0]["false_positive"] is False
    assert "email_event" in result.extra
```

在 `assert "email_event" in result.extra` 之后追加：

```python
    assert result.messages[0]["scene_id"] == "fire_door"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_smoke.py tests/test_fire_door.py -q`
Expected: 新断言 FAIL（`KeyError: 'scene_id'` 或断言不等）。

- [ ] **Step 3: 实现**

1) `backend/models/schemas.py` `AnalysisMessage` 在 `source_id: str` 之后加字段：

```python
    scene_id: str = "smoke"
```

2) `core/smoke/processor.py` 消息 dict（约 141 行）加键值（放在 `"source_id"` 之后）：

```python
                "scene_id": "smoke",
```

3) `core/fire_door/processor.py` 消息 dict（约 205 行）同样加：

```python
                    "scene_id": "fire_door",
```

4) `backend/processing/base.py` `_handle_result` 的 `AnalysisMessage(...)` 构造（约 117-132 行）在 `source_id=...` 之后加：

```python
                        scene_id=msg.get("scene_id", "smoke"),
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_smoke.py tests/test_fire_door.py tests/test_main.py -q`
Expected: 新断言 PASS，其余无新增失败。

- [ ] **Step 5: 提交**

```bash
git add backend/models/schemas.py core/smoke/processor.py core/fire_door/processor.py backend/processing/base.py tests/test_smoke.py tests/test_fire_door.py
git commit -m "feat(messages): carry scene_id through analysis message pipeline"
```

---

### Task 3: `scene_id` 消费侧 — 列表 API JOIN + 复盘上下文 db helper

**Files:**
- Modify: `backend/db/database.py`（`list_analysis_messages` 约 2578-2645 行；新函数放在 `list_analysis_messages` 之后）
- Test: `tests/test_messages.py`

**Interfaces:**
- Produces:
  - `list_analysis_messages(...)` 返回的每个 item 新增 `"scene_id": str`（无匹配源时 `"smoke"`）。
  - `async def get_analysis_message_review_context(message_id: str) -> dict[str, str] | None` — 返回 `{"source_id": str, "scene_id": str}`，消息不存在返回 `None`。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_messages.py` 的 `TestMessagePersistence` 类或同级）

```python
    async def test_list_messages_includes_scene_id(self, init_db):
        from backend.db.database import create_source
        from backend.models.schemas import VideoSourceCreate

        source = await create_source(VideoSourceCreate(name="FireCam", rtsp_url="rtsp://x/1", scene_id="fire_door"))
        await save_analysis_message(
            {
                "timestamp": datetime.now(timezone.utc).isoformat(),
                "source_name": "FireCam",
                "source_id": source.id,
                "level": "alert",
                "message": "fire door open",
                "image_base64": None,
            }
        )
        rows = await list_analysis_messages(limit=10)
        assert rows["items"][0]["scene_id"] == "fire_door"

    async def test_list_messages_scene_id_defaults_smoke_for_unknown_source(self, init_db):
        await save_analysis_message(
            {
                "timestamp": datetime.now(timezone.utc).isoformat(),
                "source_name": "Ghost",
                "source_id": "no-such-source",
                "level": "info",
                "message": "hello",
                "image_base64": None,
            }
        )
        rows = await list_analysis_messages(limit=10)
        assert rows["items"][0]["scene_id"] == "smoke"

    async def test_review_context_returns_source_and_scene(self, init_db):
        from backend.db.database import create_source, get_analysis_message_review_context
        from backend.models.schemas import VideoSourceCreate

        source = await create_source(VideoSourceCreate(name="FireCam", rtsp_url="rtsp://x/1", scene_id="fire_door"))
        message_id = await save_analysis_message(
            {
                "timestamp": datetime.now(timezone.utc).isoformat(),
                "source_name": "FireCam",
                "source_id": source.id,
                "level": "alert",
                "message": "fire door open",
                "image_base64": None,
            }
        )
        context = await get_analysis_message_review_context(message_id)
        assert context == {"source_id": source.id, "scene_id": "fire_door"}

    async def test_review_context_missing_message_returns_none(self, init_db):
        from backend.db.database import get_analysis_message_review_context

        assert await get_analysis_message_review_context("no-such-id") is None
```

注意：`tests/test_messages.py` 顶部已 import `save_analysis_message`、`list_analysis_messages`、`datetime`；`create_source` / `VideoSourceCreate` 在测试函数内按需导入（与 `tests/test_settings.py` 的用法一致）。若 `VideoSourceCreate` 不支持 `scene_id` 参数，检查 `backend/models/schemas.py` 中该模型字段（`scene_id` 在 `video_sources` 表存在，模型应有该可选字段）；如模型缺失该字段，则在模型中补 `scene_id: str | None = None` 并同步该测试。

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_messages.py -q`
Expected: 新用例 FAIL（`KeyError: 'scene_id'` / `ImportError: cannot import name 'get_analysis_message_review_context'`）。

- [ ] **Step 3: 实现**（`backend/db/database.py`）

1) `list_analysis_messages`：where 子句加 `m.` 前缀（避免 JOIN 后 `source_id` 歧义）：

```python
    if source_id:
        where_clauses.append("m.source_id = ?")
        query_values.append(source_id)
    if false_positive_filter == "only":
        where_clauses.append("m.false_positive = 1")
    elif false_positive_filter == "exclude":
        where_clauses.append("m.false_positive = 0")
```

日期两处 `created_at` 条件改为 `m.created_at >= ?` / `m.created_at < ?`。

count 查询与 listing 查询的 FROM 改为 JOIN，SELECT 追加 `COALESCE(vs.scene_id, 'smoke')`：

```python
        count_query = (
            f"SELECT COUNT(*) FROM analysis_messages m "
            f"LEFT JOIN video_sources vs ON vs.id = m.source_id{where_sql}"
        )
```

```python
        listing_query = (
            "SELECT m.id, m.timestamp, m.source_name, m.source_id, m.level, m.message, "
            "m.image_url, m.original_image_url, m.detected_image_url, m.image_base64, "
            "m.false_positive, COALESCE(vs.scene_id, 'smoke') "
            "FROM analysis_messages m "
            "LEFT JOIN video_sources vs ON vs.id = m.source_id"
            f"{where_sql} "
            "ORDER BY m.created_at DESC LIMIT ? OFFSET ?"
        )
```

items 构造追加字段（`false_positive` 之后）：

```python
            "scene_id": row[11],
```

2) 新函数（放在 `list_analysis_messages` 之后、`get_analysis_message_for_notification` 之前）：

```python
async def get_analysis_message_review_context(message_id: str) -> dict[str, str] | None:
    """Return source/scene context for one persisted message (or None).
    返回一条持久化消息的源与场景上下文（不存在时返回 None）。"""
    async with _db_session() as db:
        async with db.execute(
            "SELECT m.source_id, COALESCE(vs.scene_id, 'smoke') "
            "FROM analysis_messages m "
            "LEFT JOIN video_sources vs ON vs.id = m.source_id "
            "WHERE m.id = ?",
            (message_id,),
        ) as cursor:
            row = await cursor.fetchone()
    if row is None:
        return None
    return {"source_id": str(row[0]), "scene_id": str(row[1])}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_messages.py -q`
Expected: 全部 PASS（既有 list/过滤用例无回归）。

- [ ] **Step 5: 提交**

```bash
git add backend/db/database.py tests/test_messages.py
git commit -m "feat(messages): expose scene_id in list API and review context helper"
```

---

### Task 4: 设置页连接测试端点 `POST /api/settings/vl/test`

**Files:**
- Modify: `backend/models/schemas.py`（新增 `VlTestRequest`，放在 `EmailTestRequest`（约 608 行）之后）
- Modify: `backend/api/settings.py`（新增端点，放在 `test_email_settings` 之后）
- Modify: `backend/audit.py`（`AUDIT_OPERATION_MAP` 加一条）
- Test: `tests/test_settings.py`

**Interfaces:**
- Consumes: `core.vl_confirm.VLConfirmClient.complete`、`build_vl_test_image_data_url`、`VL_TEST_PROMPT`（Task 1）；`db.get_all_settings()`。
- Produces: `POST /api/settings/vl/test` → 200 `{"status": "ok", "model", "latency_ms", "response"}`；422（缺 base_url/model）；502（上游失败，`detail` 含原因）。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_settings.py`，放在 `test_email_test_endpoint` 之后；文件顶部需补 `from unittest.mock import AsyncMock`（如未导入）与 `from core.vl_confirm import VLConfirmClient` 不需要——用字符串路径 patch）

```python
    async def test_vl_test_endpoint_ok(self, async_client: AsyncClient):
        with patch(
            "core.vl_confirm.VLConfirmClient.complete",
            new=AsyncMock(return_value='{"connected": true}'),
        ) as mock_complete:
            resp = await async_client.post(
                "/api/settings/vl/test",
                json={
                    "vl_confirm_base_url": "http://vl.example.com/v1",
                    "vl_confirm_api_key": "test-key",
                    "vl_confirm_model": "/models/test-vl",
                    "vl_confirm_timeout": "30",
                },
            )
        assert resp.status_code == 200
        data = resp.json()
        assert data["status"] == "ok"
        assert data["model"] == "/models/test-vl"
        assert data["response"] == '{"connected": true}'
        assert isinstance(data["latency_ms"], int)
        assert mock_complete.await_count == 1

    async def test_vl_test_endpoint_missing_model_rejected(self, async_client: AsyncClient):
        resp = await async_client.post("/api/settings/vl/test", json={"vl_confirm_base_url": "http://x/v1"})
        assert resp.status_code == 422

    async def test_vl_test_endpoint_upstream_error_502(self, async_client: AsyncClient):
        with patch(
            "core.vl_confirm.VLConfirmClient.complete",
            new=AsyncMock(side_effect=Exception("401 unauthorized")),
        ):
            resp = await async_client.post(
                "/api/settings/vl/test",
                json={
                    "vl_confirm_base_url": "http://vl.example.com/v1",
                    "vl_confirm_model": "/models/test-vl",
                },
            )
        assert resp.status_code == 502
        assert "401 unauthorized" in resp.json()["detail"]
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_settings.py -q`
Expected: 3 个新用例 FAIL（404/路由不存在）。

- [ ] **Step 3: 实现**

1) `backend/models/schemas.py`：

```python
class VlTestRequest(BaseModel):
    """Payload for testing the VL backend without saving first.
    用于在不先保存的情况下测试 VL 后端的载荷。"""

    vl_confirm_base_url: str | None = None
    vl_confirm_api_key: str | None = None
    vl_confirm_model: str | None = None
    vl_confirm_timeout: str | None = None
```

2) `backend/api/settings.py` 顶部 import 补充：

```python
import time

from core.vl_confirm import VLConfirmClient, VL_TEST_PROMPT, build_vl_test_image_data_url
```

（`VlTestRequest` 加入第 8 行的 `from backend.models.schemas import ...` 列表。）

3) 在 `test_email_settings` 之后新增端点：

```python
@router.post("/vl/test")
async def test_vl_settings(
    data: VlTestRequest,
    _role: str = Depends(require_any_permission("settings:*", "settings:plugins")),
) -> dict[str, object]:
    """Run a full connection test against the configured VL backend.
    使用当前或传入的设置对 VL 后端做一次全链路连接测试。"""
    app_settings = await db.get_all_settings()
    base_url = str(data.vl_confirm_base_url or app_settings.get("vl_confirm_base_url") or "").strip()
    api_key = str(data.vl_confirm_api_key or app_settings.get("vl_confirm_api_key") or "").strip()
    model = str(data.vl_confirm_model or app_settings.get("vl_confirm_model") or "").strip()
    timeout_raw = str(data.vl_confirm_timeout or app_settings.get("vl_confirm_timeout") or "60")
    if not base_url or not model:
        raise HTTPException(status_code=422, detail="VL base URL and model are required")
    try:
        timeout = max(1, int(float(timeout_raw)))
    except ValueError:
        timeout = 60
    client = VLConfirmClient(base_url=base_url, api_key=api_key or "EMPTY", model=model, timeout=timeout)
    started = time.monotonic()
    try:
        raw = await client.complete(build_vl_test_image_data_url(), VL_TEST_PROMPT)
    except Exception as exc:
        raise HTTPException(status_code=502, detail=f"VL request failed: {exc}")
    return {
        "status": "ok",
        "model": model,
        "latency_ms": int((time.monotonic() - started) * 1000),
        "response": raw,
    }
```

4) `backend/audit.py` `AUDIT_OPERATION_MAP` 在 `("POST", "/api/settings/email/test")` 行之后加：

```python
    ("POST", "/api/settings/vl/test"): ("settings.vl_test", "settings"),
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_settings.py -q`
Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add backend/models/schemas.py backend/api/settings.py backend/audit.py tests/test_settings.py
git commit -m "feat(settings): add VL backend connection test endpoint"
```

---

### Task 5: 单消息复盘端点 `POST /api/messages/{id}/vl-review`

**Files:**
- Modify: `backend/api/messages.py`（新增端点，放在 `resend_message_notification` 之后）
- Modify: `backend/audit.py`（加一条）
- Test: `tests/test_messages.py`

**Interfaces:**
- Consumes: Task 1（`VLConfirmClient.complete`、`parse_vl_response`、`encode_frame_as_data_url`）、Task 3（`db.get_analysis_message_review_context`、`db.get_analysis_message_image_path`）、`db.get_all_settings()`、`core.smoke.constants.DEFAULT_VL_CONFIRM_PROMPT/RESPONSE_KEY`、`core.fire_door.constants.DEFAULT_VL_CONFIRM_PROMPT/RESPONSE_KEY`。
- Produces: `POST /api/messages/{message_id}/vl-review` → 200 `{"result": "confirmed|rejected|unknown", "raw_response", "latency_ms", "model"}`；404（消息/图片不存在）；422（VL 未启用/端点未配置）；502（上游失败）。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_messages.py`；文件顶部 import 区补 `import cv2`、`import numpy as np`——`base64`、`datetime`、`AsyncMock`、`patch`、`AsyncClient`、`save_analysis_message`、`list_analysis_messages`、`update_settings` 均已在该文件导入）

说明：消息必须携带**真实 JPEG** 的 `image_base64`（端点会对落盘文件执行 `cv2.imread`）。`save_analysis_message` 会把 base64 物化到与测试临时 DB 同级的消息图片目录，`get_analysis_message_image_path` 再解析回来，无需任何 monkeypatch。

```python
VL_REVIEW_JPEG_B64 = base64.b64encode(
    cv2.imencode(".jpg", np.zeros((32, 48, 3), dtype=np.uint8))[1].tobytes()
).decode("ascii")


class TestVlReviewEndpoint:
    async def _save_message_with_image(self, source_id: str) -> str:
        return await save_analysis_message(
            {
                "timestamp": datetime.now(timezone.utc).isoformat(),
                "source_name": "Cam1",
                "source_id": source_id,
                "level": "alert",
                "message": "smoke detected",
                "image_base64": VL_REVIEW_JPEG_B64,
            }
        )

    async def _create_source(self):
        from backend.db.database import create_source
        from backend.models.schemas import VideoSourceCreate

        return await create_source(VideoSourceCreate(name="Cam1", rtsp_url="rtsp://x/1"))

    async def test_vl_review_confirmed(self, async_client: AsyncClient, init_db):
        source = await self._create_source()
        message_id = await self._save_message_with_image(source.id)
        await update_settings({"smoke_vl_confirm_enabled": "true"})

        with patch("core.vl_confirm.VLConfirmClient.complete", new=AsyncMock(return_value='{"smoke": true}')):
            resp = await async_client.post(f"/api/messages/{message_id}/vl-review")
        assert resp.status_code == 200
        data = resp.json()
        assert data["result"] == "confirmed"
        assert data["raw_response"] == '{"smoke": true}'
        assert isinstance(data["latency_ms"], int)
        # 复盘不得改变消息状态
        rows = await list_analysis_messages(limit=10)
        assert rows["items"][0]["false_positive"] is False

    async def test_vl_review_unknown_verdict(self, async_client: AsyncClient, init_db):
        source = await self._create_source()
        message_id = await self._save_message_with_image(source.id)
        await update_settings({"smoke_vl_confirm_enabled": "true"})

        with patch("core.vl_confirm.VLConfirmClient.complete", new=AsyncMock(return_value="cannot tell")):
            resp = await async_client.post(f"/api/messages/{message_id}/vl-review")
        assert resp.status_code == 200
        assert resp.json()["result"] == "unknown"

    async def test_vl_review_disabled_scene_422(self, async_client: AsyncClient, init_db):
        source = await self._create_source()
        message_id = await self._save_message_with_image(source.id)

        resp = await async_client.post(f"/api/messages/{message_id}/vl-review")
        assert resp.status_code == 422

    async def test_vl_review_missing_message_404(self, async_client: AsyncClient, init_db):
        resp = await async_client.post("/api/messages/no-such-id/vl-review")
        assert resp.status_code == 404

    async def test_vl_review_missing_image_404(self, async_client: AsyncClient, init_db):
        source = await self._create_source()
        message_id = await save_analysis_message(
            {
                "timestamp": datetime.now(timezone.utc).isoformat(),
                "source_name": "Cam1",
                "source_id": source.id,
                "level": "alert",
                "message": "smoke detected",
                "image_base64": None,
            }
        )
        await update_settings({"smoke_vl_confirm_enabled": "true"})

        resp = await async_client.post(f"/api/messages/{message_id}/vl-review")
        assert resp.status_code == 404

    async def test_vl_review_upstream_error_502(self, async_client: AsyncClient, init_db):
        source = await self._create_source()
        message_id = await self._save_message_with_image(source.id)
        await update_settings({"smoke_vl_confirm_enabled": "true"})

        with patch("core.vl_confirm.VLConfirmClient.complete", new=AsyncMock(side_effect=Exception("conn refused"))):
            resp = await async_client.post(f"/api/messages/{message_id}/vl-review")
        assert resp.status_code == 502
        assert "conn refused" in resp.json()["detail"]
```

注意：`async_client` fixture 应已包含登录态（`tests/test_messages.py` 既有用例直接调写接口）。`create_source` / `VideoSourceCreate` 在辅助方法内按需导入，与 `tests/test_settings.py` 的用法一致。

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_messages.py -q`
Expected: `TestVlReviewEndpoint` 全部 FAIL（404 路由不存在；disabled 用例可能偶然 404 后改为 422 失败）。

- [ ] **Step 3: 实现**

1) `backend/api/messages.py` 顶部 import 补充：

```python
import time

import cv2

from backend.db import database as db
from core.fire_door.constants import (
    DEFAULT_VL_CONFIRM_PROMPT as FIRE_DOOR_DEFAULT_VL_PROMPT,
    DEFAULT_VL_CONFIRM_RESPONSE_KEY as FIRE_DOOR_DEFAULT_VL_RESPONSE_KEY,
)
from core.smoke.constants import (
    DEFAULT_VL_CONFIRM_PROMPT as SMOKE_DEFAULT_VL_PROMPT,
    DEFAULT_VL_CONFIRM_RESPONSE_KEY as SMOKE_DEFAULT_VL_RESPONSE_KEY,
)
from core.vl_confirm import VLConfirmClient, encode_frame_as_data_url, parse_vl_response
```

并在 `from backend.db.database import (...)` 列表中加入 `get_analysis_message_review_context`。

2) 在 `resend_message_notification` 之后新增端点：

```python
SCENE_VL_DEFAULTS = {
    "smoke": (SMOKE_DEFAULT_VL_PROMPT, SMOKE_DEFAULT_VL_RESPONSE_KEY),
    "fire_door": (FIRE_DOOR_DEFAULT_VL_PROMPT, FIRE_DOOR_DEFAULT_VL_RESPONSE_KEY),
}


@router.post("/{message_id}/vl-review")
async def review_message_with_vl(
    message_id: str,
    _role: str = Depends(require_permission("messages:annotate")),
) -> dict[str, object]:
    """Re-run VL confirmation on a persisted message. Display-only.
    对一条已持久化消息重跑 VL 复盘（只展示结果，不修改消息状态）。"""
    context = await get_analysis_message_review_context(message_id)
    if context is None:
        raise HTTPException(status_code=404, detail="Message not found")
    scene_id = context["scene_id"]
    settings_map = await db.get_all_settings()
    enabled = str(settings_map.get(f"{scene_id}_vl_confirm_enabled") or "false").strip().lower()
    if enabled != "true":
        raise HTTPException(
            status_code=422,
            detail=f"VL confirmation is not enabled for scene '{scene_id}'",
        )
    base_url = str(settings_map.get("vl_confirm_base_url") or "").strip()
    model = str(settings_map.get("vl_confirm_model") or "").strip()
    if not base_url or not model:
        raise HTTPException(status_code=422, detail="VL base URL and model are required")
    try:
        timeout = max(1, int(float(settings_map.get("vl_confirm_timeout") or 60)))
    except ValueError:
        timeout = 60

    image_source = str(settings_map.get(f"{scene_id}_vl_confirm_image_source") or "original").strip().lower()
    kind = "detected" if image_source == "annotated" else "original"
    file_path = await get_analysis_message_image_path(message_id, kind=kind)
    if file_path is None or not file_path.is_file():
        raise HTTPException(status_code=404, detail="Message image not found")
    frame_bgr = cv2.imread(str(file_path), cv2.IMREAD_COLOR)
    if frame_bgr is None:
        raise HTTPException(status_code=404, detail="Message image not found")
    frame_rgb = cv2.cvtColor(frame_bgr, cv2.COLOR_BGR2RGB)
    image_data_url = encode_frame_as_data_url(frame_rgb)

    prompt = str(settings_map.get(f"{scene_id}_vl_confirm_prompt") or "").strip()
    response_key = str(settings_map.get(f"{scene_id}_vl_confirm_response_key") or "").strip()
    default_prompt, default_response_key = SCENE_VL_DEFAULTS.get(
        scene_id, (SMOKE_DEFAULT_VL_PROMPT, SMOKE_DEFAULT_VL_RESPONSE_KEY)
    )
    prompt = prompt or default_prompt
    response_key = response_key or default_response_key

    client = VLConfirmClient(
        base_url=base_url,
        api_key=str(settings_map.get("vl_confirm_api_key") or "EMPTY").strip() or "EMPTY",
        model=model,
        timeout=timeout,
    )
    started = time.monotonic()
    try:
        raw = await client.complete(image_data_url, prompt)
    except Exception as exc:
        raise HTTPException(status_code=502, detail=f"VL request failed: {exc}")
    latency_ms = int((time.monotonic() - started) * 1000)
    verdict = parse_vl_response(raw, response_key)
    result = "confirmed" if verdict is True else ("rejected" if verdict is False else "unknown")
    return {
        "result": result,
        "raw_response": raw,
        "latency_ms": latency_ms,
        "model": model,
    }
```

3) `backend/audit.py` 在 `("POST", "/api/messages/{message_id}/resend-notification")` 行之后加：

```python
    ("POST", "/api/messages/{message_id}/vl-review"): ("messages.vl_review", "messages"),
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_messages.py -q`
Expected: 全部 PASS（含既有用例）。

- [ ] **Step 5: 全套回归 + 提交**

Run: `uv run pytest tests -q`（无新增失败）

```bash
git add backend/api/messages.py backend/audit.py tests/test_messages.py
git commit -m "feat(messages): add per-message VL review endpoint"
```

---

### Task 6: 前端 API / store / i18n

**Files:**
- Modify: `frontend/src/api/index.js`
- Modify: `frontend/src/stores/appSettings.js`
- Modify: `frontend/src/stores/message.js`
- Modify: `frontend/src/stores/__tests__/message.test.js`
- Modify: `frontend/src/i18n/locales/zh-CN.js`、`frontend/src/i18n/locales/en-US.js`

**Interfaces:**
- Produces（Task 7/8 依赖）:
  - `settingsApi.testVl(data)`、`appSettingsStore.testVl(payload)`
  - `messagesApi.vlReview(id)`、`messageStore.vlReviewMessage(messageId)`
  - i18n 键：`messages.vlReviewResultTitle` / `vlReviewConfirmed` / `vlReviewRejected` / `vlReviewUnknown` / `vlReviewLatency` / `vlReviewFailed`；`messageList.vlReview`；`settings.vlTestConnection` / `vlTestSuccess` / `vlTestFailed`。

- [ ] **Step 1: 写失败测试**（`frontend/src/stores/__tests__/message.test.js`：顶部 `vi.mock('../../api/index.js', ...)` 的 `messagesApi` 工厂里加 `vlReview: () => vlReviewMock`，并在 `listMock` 旁声明 `const vlReviewMock = vi.fn()`；`beforeEach` 的 reset 列表里加 `vlReviewMock.mockReset()`；文件末尾追加）

```js
describe('message store — vl review', () => {
  it('vlReviewMessage calls the API and returns the result', async () => {
    const store = useMessageStore()
    vlReviewMock.mockResolvedValue({ result: 'confirmed', raw_response: '{"smoke": true}', latency_ms: 120, model: '/models/Mage-VL' })
    const result = await store.vlReviewMessage('id-9')
    expect(vlReviewMock).toHaveBeenCalledWith('id-9')
    expect(result.result).toBe('confirmed')
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npx vitest run src/stores/__tests__/message.test.js`
Expected: 新用例 FAIL（`store.vlReviewMessage is not a function`）。

- [ ] **Step 3: 实现**

1) `frontend/src/api/index.js`：

`messagesApi` 对象内（`resendNotification` 之后）加：

```js
  vlReview: (id) => api.post(`/api/messages/${id}/vl-review`),
```

`settingsApi` 对象内（`testEmail` 之后）加：

```js
  testVl: (data) => api.post('/api/settings/vl/test', data),
```

2) `frontend/src/stores/appSettings.js`（参照 `testEmail`，约 104 行）加：

```js
  async function testVl(payload) {
    return settingsApi.testVl(payload)
  }
```

并在 return 对象中（`testEmail,` 之后）导出 `testVl`。

3) `frontend/src/stores/message.js`（`resendNotification` 之后）加：

```js
  async function vlReviewMessage(messageId) {
    return messagesApi.vlReview(messageId)
  }
```

并在 return 对象中（`resendNotification,` 之后）导出 `vlReviewMessage`。

4) i18n——`frontend/src/i18n/locales/zh-CN.js`：

`messages` 段（`resendNotificationFailed` 行之后）加：

```js
    vlReviewResultTitle: 'VL 复盘结果',
    vlReviewConfirmed: '模型判定：确认（有效告警）',
    vlReviewRejected: '模型判定：误报',
    vlReviewUnknown: '模型判定：无法判定',
    vlReviewLatency: '耗时：{ms} ms',
    vlReviewFailed: 'VL 复盘失败：{message}',
```

`messageList` 段（`resendNotification` 行之后）加：

```js
    vlReview: 'VL 复盘',
```

`settings` 段（`testEmailFailed` 行之后）加：

```js
    vlTestConnection: '测试连接',
    vlTestSuccess: 'VL 连接测试成功（{latency} ms）：{response}',
    vlTestFailed: 'VL 连接测试失败：{message}',
```

`frontend/src/i18n/locales/en-US.js` 相同位置加：

```js
    vlReviewResultTitle: 'VL Review Result',
    vlReviewConfirmed: 'Model verdict: confirmed (valid alert)',
    vlReviewRejected: 'Model verdict: false positive',
    vlReviewUnknown: 'Model verdict: undetermined',
    vlReviewLatency: 'Latency: {ms} ms',
    vlReviewFailed: 'VL review failed: {message}',
```

```js
    vlReview: 'VL Review',
```

```js
    vlTestConnection: 'Test Connection',
    vlTestSuccess: 'VL connection test succeeded ({latency} ms): {response}',
    vlTestFailed: 'VL connection test failed: {message}',
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npx vitest run src/stores/__tests__/message.test.js`
Expected: 全部 PASS（既有 + 新）。

- [ ] **Step 5: 提交**

```bash
git add frontend/src/api/index.js frontend/src/stores/appSettings.js frontend/src/stores/message.js frontend/src/stores/__tests__/message.test.js frontend/src/i18n/locales/zh-CN.js frontend/src/i18n/locales/en-US.js
git commit -m "feat(settings): add VL test/review API, stores, and i18n"
```

---

### Task 7: 设置页「测试连接」按钮

**Files:**
- Modify: `frontend/src/views/Settings.vue`

**Interfaces:**
- Consumes: Task 6 的 `appSettingsStore.testVl` 与 i18n 键。
- Produces: 无（UI 终点）。

- [ ] **Step 1: 实现**

1) `const testingEmail = ref(false)`（约 1495 行）之后加：

```js
const testingVl = ref(false)
```

2) `testEmailConfig` 函数（约 2184-2207 行）之后加：

```js
async function testVlConfig() {
  testingVl.value = true
  try {
    const payload = {
      vl_confirm_base_url: form.value.vl_confirm_base_url,
      vl_confirm_api_key: form.value.vl_confirm_api_key,
      vl_confirm_model: form.value.vl_confirm_model,
      vl_confirm_timeout: form.value.vl_confirm_timeout,
    }
    const result = await appSettingsStore.testVl(payload)
    ElMessage.success(
      t('settings.vlTestSuccess', {
        latency: result.latency_ms,
        response: String(result.response || '').slice(0, 80),
      })
    )
  } catch (err) {
    ElMessage.error(t('settings.vlTestFailed', { message: err.message }))
  } finally {
    testingVl.value = false
  }
}
```

3) 模板：smoke VL 卡片（`<h2>{{ t('settings.vlConfirmTitle') }}</h2>` 所在 `section-card__head`，约 822-828 行）的 `</div>`（`info-tip` 所在 div）之后、`section-card__head` 闭合 `</div>` 之前加：

```html
                      <div class="section-card__actions">
                        <el-button :loading="testingVl" @click="testVlConfig">
                          {{ t('settings.vlTestConnection') }}
                        </el-button>
                      </div>
```

4) fire_door 的 VL 卡片（同结构，约 950 行附近，`vlConfirmTitle` 同键）同样加一段相同按钮。

- [ ] **Step 2: 构建验证**

Run: `cd frontend && npm run build`
Expected: 构建成功，无 ESLint/模板错误。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/views/Settings.vue
git commit -m "feat(settings): add VL connection test buttons"
```

---

### Task 8: 消息页「VL 复盘」按钮 + 结果对话框

**Files:**
- Modify: `frontend/src/components/MessageList.vue`
- Modify: `frontend/src/views/Messages.vue`

**Interfaces:**
- Consumes: Task 6 的 `messageStore.vlReviewMessage`、i18n 键、`AnalysisMessage.scene_id`（Task 2/3）。
- Produces: 无（UI 终点）。

- [ ] **Step 1: 实现 `MessageList.vue`**

1) `defineProps` 的 `resendingMessageIds` 之后加：

```js
  reviewingMessageIds: {
    type: Object,
    default: () => ({}),
  },
```

2) `defineEmits` 数组加 `'vl-review'`（放在 `'resend-notification'` 之后）。

3) `canDeleteMessages` computed 之后加显隐逻辑：

```js
function vlReviewVisible(message) {
  if (!canAnnotateMessages.value || !message?.id) return false
  if (!message.source_id || message.source_id === '__agent__') return false
  const scene = message.scene_id || 'smoke'
  const enabled = appSettingsStore.settings?.[`${scene}_vl_confirm_enabled`]
  return String(enabled || 'false').toLowerCase() === 'true'
}
```

4) 模板：`resend-notification` 按钮之后、`mark-false-positive` 按钮之前加：

```html
          <el-button
            v-if="vlReviewVisible(msg)"
            size="small"
            type="primary"
            plain
            :loading="Boolean(reviewingMessageIds[msg.id])"
            @click="emit('vl-review', msg)"
          >
            {{ t('messageList.vlReview') }}
          </el-button>
```

- [ ] **Step 2: 实现 `Messages.vue`**

1) `const resendingMessageIds = ref({})` 之后加：

```js
const reviewingMessageIds = ref({})
```

2) `handleResendNotification` 函数之后加：

```js
function escapeHtml(value) {
  const map = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }
  return String(value).replace(/[&<>"']/g, (ch) => map[ch])
}

async function handleVlReview(message) {
  if (!message?.id) return
  reviewingMessageIds.value = { ...reviewingMessageIds.value, [message.id]: true }
  try {
    const result = await store.vlReviewMessage(message.id)
    const verdictKey =
      { confirmed: 'vlReviewConfirmed', rejected: 'vlReviewRejected', unknown: 'vlReviewUnknown' }[result.result] ||
      'vlReviewUnknown'
    await ElMessageBox.alert(
      `<div style="white-space:pre-wrap;word-break:break-word">${t(`messages.${verdictKey}`)}\n${t('messages.vlReviewLatency', { ms: result.latency_ms })}\n\n${escapeHtml(result.raw_response || '')}</div>`,
      t('messages.vlReviewResultTitle'),
      { dangerouslyUseHTMLString: true, confirmButtonText: t('common.confirm') }
    )
  } catch (err) {
    if (err && (err === 'cancel' || err?.message === 'cancel')) return
    ElMessage.error(t('messages.vlReviewFailed', { message: err.message }))
  } finally {
    const next = { ...reviewingMessageIds.value }
    delete next[message.id]
    reviewingMessageIds.value = next
  }
}
```

注：`common.confirm` 键已确认存在（`zh-CN.js` `confirm: '确认'`、`en-US.js` `confirm: 'Confirm'`，均在 `common` 段）。

3) 模板 `<MessageList ...>`（约 79-89 行）加两个属性（`@resend-notification` 之后）：

```html
        :reviewing-message-ids="reviewingMessageIds"
        @vl-review="handleVlReview"
```

- [ ] **Step 3: 构建 + 测试验证**

Run: `cd frontend && npm run build && npx vitest run`
Expected: 构建成功；测试无新增失败（基线 2 个既有失败除外）。

- [ ] **Step 4: 提交**

```bash
git add frontend/src/components/MessageList.vue frontend/src/views/Messages.vue
git commit -m "feat(messages): add per-message VL review button and result dialog"
```

---

### Task 9: 全量回归收尾

**Files:** 无新增改动（仅验证；如有小修则随本任务提交）。

**Interfaces:** 无。

- [ ] **Step 1: 后端全套**

Run: `uv run pytest tests -q`
Expected: 无新增失败（基线 1 个既有 `test_main.py` 失败除外）。

- [ ] **Step 2: 前端全套 + 构建**

Run: `cd frontend && npx vitest run && npm run build`
Expected: 无新增失败（基线 2 个既有 `settingsRoutes.test.js` 失败除外）；构建成功。

- [ ] **Step 3: 如有修复，提交；否则跳过**

```bash
git add -A && git commit -m "test: fix regressions from VL review feature"   # 仅在有改动时
```

## 手动 E2E（由用户在真实 VL 后端环境执行）

1. 设置页插件 VL 卡片点「测试连接」→ 成功：提示耗时 + 模型回复；故意填错 base_url → 提示具体错误（连接失败/401/404）。
2. 启用 smoke 场景 VL 后，消息卡片出现「VL 复盘」按钮 → 点击 → 对话框显示判定/原始回复/耗时；消息 `false_positive` 状态不变。
3. fire_door 场景未启用 VL 时，其消息无「VL 复盘」按钮；`__agent__` 汇总消息无按钮。
4. 删除过/图片已清理的消息点复盘 → 提示「图片不存在」。
